# Shell scripts must keep LF line endings even when checked out on Windows --
# CRLF can make shell scripts fail with "bad interpreter" on
# Linux / macOS. PowerShell, markdown, and other text default to git's auto
# line-ending behavior.
*.sh text eol=lf
